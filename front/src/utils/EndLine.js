import "./EndLine.css"

export default function EndLine(props) {

    return (
        <div className={"end-line"}>
            <i className={"page-end"}>
                END
            </i>
        </div>
    );
}
